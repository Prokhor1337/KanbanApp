services:
  postgres:
    image: postgres:16-alpine
    container_name: kanban_db
    environment:
      - POSTGRES_DB=KanbanDb
      - POSTGRES_USER=kanban
      - POSTGRES_PASSWORD=chertilin20061337!
    ports:
      - "5432:5432"
    volumes:
      - kanban_db_data:/var/lib/postgresql/data

volumes:
  kanban_db_data: